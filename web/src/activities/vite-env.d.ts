/* Vite client types (import.meta.env) — needed for the compile-time DEV guard
 * that keeps the renderer sandbox out of production builds. Lives here because
 * tsconfig includes all of src/ and the project had no vite-env.d.ts yet.
 */
/// <reference types="vite/client" />
