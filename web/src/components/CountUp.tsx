/* DESIGN-002 names CountUp a component; the implementation lives beside Reveal
 * in activities/motion so both share one reduced-motion path. Re-exported here
 * so consumers can import it from either place. */
export { CountUp } from "../activities/motion";
