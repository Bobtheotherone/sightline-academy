# Crawl pass 3 review — Wave 2 exit (run qa/crawl-runs/20260806-065215)

77 screenshots, 1 skip. Reviewed against DESIGN-006. All 12 renderer types now
capture with REAL data and REAL art; the two showcase labs genuinely showcase
(stability explorer: dual-view CoG model with live objectives; walkaround:
two-stage zone placement over a bespoke top-down machine). Certificate is a
true diploma (seal, display name, mono code, verbatim disclaimer). Progress
shows all nine real badge plates in blaze frames. Hotspot markers sit on their
named features after the 5:3 frame fix.

## Findings
- SKIP renderer-content (fresh, m1-l1-s1): first /learn navigation cold-compiles
  the lesson chunk graph in dev and outran the harness patience window. Harness
  now waits for player mount (wait_for_selector) — capture at the next pass.
- P3 certificate issued date renders the grad fixture's deterministic backdate
  (Jul 30) — fixture behavior, not product.
- P3 hotspot side-panel empty-state ("Pick a waypoint") occupies a tall column
  at 1440 before first tap — acceptable, revisit in final polish if it grates.
- Deep states not yet reachable by the harness (assessment in-progress/review/
  results, streaming mid-token, wrong-answer feedback frames, loading skeletons,
  error toast, offline banner, focus-visible tab-through): Wave 3 Design QA
  extends drive_state per QA-001 §crawl items 3/5 and iterates to zero P1/P2.

## Verdict
Wave 2 exit criteria met: full course completable (verified twice by playthrough
lanes + the capstone journey), certificate renders and verifies publicly, crawl
pass 3 produced and reviewed. No P1s. Remaining polish routed to Wave 3.
