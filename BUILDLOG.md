# BUILDLOG — Sightline Safety Academy

One line per meaningful decision or deviation (AGENT_OPERATIONS §BUILDLOG format).

2026-08-06 W0 [orchestrator] Repo root = D:\Sightline_Saftey_Academy (handoff folder kept in-repo as reference per START_HERE; content/ copies are the live source of truth per ADR-002).
2026-08-06 W0 [orchestrator] Ingest module path: server/app/ingest/ingest.py (STARTER/README + RAG_CORPUS/README say app/ingest; ADR-002 tree draws server/ingest — majority wins, noting the mismatch here).
2026-08-06 W0 [orchestrator] Local dev Python pinned to 3.12 via uv per ADR-001; system 3.13 not used for the server venv.
2026-08-06 W0 [content] Pre-flight parse of content/curriculum: 6 modules, 22 lessons, 59 steps, all 12 renderer types present, all payload JSON valid, every knowledge check exactly one isBest.
2026-08-06 W0 [deviate] SPEC-003 says "every lesson has a checkpoint step" but 8/22 authored lessons have none and SPEC-000 says not every lesson has every section. Content is complete and binding — seed parser will validate "checkpoint, where present, is the last step" instead of requiring one per lesson.
2026-08-06 W0 [deviate] Live crawl manifest is qa/route-manifest.json (QA-001 says copy into repo; kept beside the crawl script rather than a root STARTER/ dir). Fixed two wrong ids vs curriculum: m4-l1-stability/m4-l1-s2 → m4-l2-stability/m4-l2-s2; m6-l4-capstone → m6-l4-ride-plan.
2026-08-06 W0 [server] Email validated with a conservative regex (email-validator dep not added); rate limiter counts failed attempts only, one shared per-IP bucket for login+register, 429 at 8th failure/5min.
2026-08-06 W0 [server] /auth/export excludes password_hash and session rows (ADR-003 "never return hashes" overrides "full dump"); DELETE confirm mismatch → 400 confirm_mismatch; wrong current password → 400 wrong_password.
2026-08-06 W0 [server] health reports chroma docs 0 without importing chromadb until ingest lands; UserOut.level fixed at 1 until SPEC-009 lands; instructor role re-checked (upgrade only) at login per SPEC-005.
2026-08-06 W0 [server] ApiError in app/errors.py (avoids auth↔main circular import); ruff select pinned (E4,E7,E9,F,I,B,UP,RUF; B008 ignored for FastAPI Depends); schemas.py 406 lines — single-file SPEC-004 contract per ADR-002.
2026-08-06 W0 [web] api.ts 417 lines — SPEC-004 contract discipline wants the whole contract in one file; typescript-eslint added so lint actually parses TS (no strict campaign).
2026-08-06 W0 [web] .ts-contour-dark added in app.css (paper-0 strokes 7%) for pine-950 heroes; tokens.css untouched; Tailwind default color/font namespaces wiped in @theme so library defaults can't leak (DESIGN-006).
2026-08-06 W0 [web] Favicon/contour-dark data URIs carry literal palette hex — ADR-008 no-hex rule applied to components; stylesheets/assets are the token layer.
2026-08-06 W0 [web] Until course/progress APIs land: modules 2–6 render designed locked state, dashboard/course/progress show first-run compositions; journal GET 404 renders the DESIGN-005 empty composition.
2026-08-06 W0 [ops] content/ COPYed into api image (ADR-006 content-as-code; edit ⇒ rebuild); image mirrors monorepo layout so ../content resolves as in dev.
2026-08-06 W0 [ops] .env.example comments moved to own lines — compose env parser would have made an inline comment the literal ANTHROPIC_API_KEY value and silently selected the anthropic provider.
2026-08-06 W0 [ops] /api proxy: proxy_buffering off + 75s read timeout so SSE streaming survives nginx; security headers repeated in /assets/ (add_header inheritance reset); compose project name pinned "sightline"; MiniLM pre-downloaded at image build into /opt/hf-cache.
2026-08-06 W0 [qa] visual_crawl.py login locator anchored (^password$) — unanchored regex also matched the PasswordInput "Show password" toggle (Playwright strict mode). Minimal STARTER-copy fix per copy rule.
2026-08-06 W0 [qa] Crawl pass 1: 66 shots, 12 skips (all /learn renderer states — no seed yet). Review in artifacts/crawl/pass-1/REVIEW.md. Wave 0 exit criteria met.
2026-08-06 W1 [content][deviate] Checkpoint rule refined again against real content: m6-l3 authors a debrief reflection AFTER its checkpoint → parser validates "≤1 checkpoint per lesson; only debrief-section steps may follow it".
2026-08-06 W1 [content] Lesson/step yaml blocks parsed as flat key:value (authored summaries in m6 aren't strict YAML); module front-matter still yaml.safe_load. assessment_bank JSON column added to course_meta; xp_events gained nullable ref column for award-once dedupe. pyyaml promoted to explicit dep.
2026-08-06 W1 [content] Fixtures: mid = 17 legal-rule XP events summing exactly 340 (route-manifest number wins over engine replay ~605); grad = honest replay 1990 XP; grad certificate code deterministic (sha256 email → crockford-32) so /verify crawl targets survive rebuilds; mid keeps one non-first-try checkpoint for instructor stats.
2026-08-06 W1 [server] SPEC-004 "modules include per-user {…}" resolved FLAT matching api.ts; spec file updated (contract discipline). GET /progress returns all 9 badges with awardedAt null when unearned. Locking is module-level server-side (403 module_locked w/ DESIGN-005 copy); within-module lesson order enforced client-side.
2026-08-06 W1 [server] ride_plan completion fires journal_artifact_complete 30 AND capstone_complete 100 (separate SPEC-009 events, ref-deduped). learner_state updates on every evidence PUT incl. incomplete saves (that IS the step-visit signal). Evidence completion is sticky — revisits never downgrade. health reports status "degraded" when chroma docs = 0.
2026-08-06 W1 [server] scenario_best_path awards on a traversal whose re-offered nodes ended all-best (risky-then-best re-offer counts as the revisit SPEC-009 allows) — settled, no change.
2026-08-06 W1 [tutor] SYSTEM_PROMPT byte-identical to STARTER (diff-verified); anthropic provider = direct httpx Messages API call (no SDK dep), 30s timeout → 504 tutor_timeout with DESIGN-005 copy; extractive legal_specific fills {RULE_CATEGORY} from top chunk topic map; user turns persist grounding='none'; suggestions not persisted (no SPEC-003 column) — history replays without buttons.
2026-08-06 W1 [tutor] SOFT_FLOOR tuned once: 0.30 → 0.43 (observed MiniLM cosine bands on this corpus: direct hits .455–.715, noise ≤.411; floor sits in the gap). Recorded per SPEC-008.
2026-08-06 W1 [tutor][deviate] Acceptance Q2 (snorkel) labels "mixed", not the spec-expected "general": corpus chunk terrain-water-crossings is genuinely relevant, so "partially supported + beyond-the-course note" is the honest ADR-005 label. Substance passes (risk-aware answer, no refusal, M4 bridge).
2026-08-06 W1 [content] Added corpus chunk machine-walkaround-no-gos (33rd; sanctioned by RAG_CORPUS/README) so acceptance Q1 retrieves two above-floor M2 chunks → grounding=curriculum with 2 chips. Verified live: Q1 curriculum/2 chips, Q3 curriculum/roads-led.
2026-08-06 W1 [web] marked added as the one markdown dependency (trusted authored content, single Markdown component). Renderer sandbox at /dev/renderers is DEV-only (verified absent from prod dist). journal_builder completion also requires non-empty fields; reflection completes on chip OR ≥3-char text.
2026-08-06 W1 [web] Course map rebuilt as winding TrailPath SVG with pine-300 fill per completed module (+ a seventh "summit" waypoint for assessment/certificate — DESIGN-003 silent on trail end); landing trail matches. Continue deep-link semantics: ?step honored when frontier/review/edit, else first incomplete. Client mirrors SPEC-009 thresholds solely for the level-up toast; server stays authoritative.
2026-08-06 W1 [web] /tutor/ask/stream deferred to Wave 2 (extractive can't stream). TutorMessageOut.sourceRefs added to spec + api.ts (read-time chip enrichment). History tie-break: role-asc on equal created_at (Windows clock ties rendered pairs inverted).
2026-08-06 W1 [orchestrator] Wave 1 exit verified: Module 1 played start-to-finish in-browser by a fresh account (incl. wrong-answer, risky-branch, journal paths); acceptance Q1/Q3 spec-exact + Q2 settled; repo-wide tsc/ruff/pytest green (59 tests).
2026-08-06 W1 [qa] Crawl harness matured for deep states: LessonPage stage exposes data-step-id; drive_state uses ?step deep links + interstitial dismissal + generic activity driving (button cycling) + patience window before SKIP; stability state fixture mid→grad (m4 locked for mid). One self-inflicted JSX-comment syntax error caught and fixed.
2026-08-06 W1 [qa] Crawl pass 2: 77 shots, 1 skip (renderer-content raced a lazy chunk — patience window added after the run; probe-verified). Review in artifacts/crawl/pass-2/REVIEW.md. P2s → Wave 2 briefs: recent-XP truncation, slot art, percent-display consistency.
2026-08-06 W2 [server] GET /assessment/final added (sanitized bank feed — SPEC-004 had no question-delivery row; page can't render without one). /verify invalid code → 200 {valid:false} with crockford lookalike folding (I/L→1, O→0). Assessment: unanswered/unknown answers fall back to correct-option feedback; pass ≥80.0; retakes never double-award; fixture grad code untouched.
2026-08-06 W2 [server] SSE /tutor/ask/stream: anthropic real deltas / extractive 10-word chunks, ONE meta event; token emission holds at a ``` fence so in-band suggestions never stream; post-start errors → one envelope-shaped error frame, nothing persisted. InstructorOverviewOut extended additively (certificatesIssued, medianModulesCompleted, triageCounts; explicit activeLast7d alias). Dev J2 helper POST /dev/complete-module mounted only when FIXTURES=1, writes honest evidence through the real progress service.
2026-08-06 W2 [web] Labs are bespoke inline-SVG showcases (config.assetSlot ignored; scene-walkaround-top marked lab-owned). Stability model constants: machine .68@.66m, rider .32@1.2m movable, cargo +.18@.85m → edge 29.0° no-lean / 33.4° full-lean; About-popover declares concept-model per SPEC-007. structured_response criteria chips = progressive length + keyword echoes, coaching only; checkpoint written mode: reviseCopy as "Coach's note" while drafting.
2026-08-06 W2 [art] 26 original SVG plates (7 heroes, 5 scenes, 9 badges, 4 states, cert-seal) in web/src/assets/svg; manifest upgraded to {status, file, alt} as SlotArt's single source of truth; hotspot bases authored 500×300 with features under the exact authored x/y; all plates ≤6 token colors, 1.5px linework, no ids/defs; largest 5.6KB.
2026-08-06 W2 [qa-fix] course-verify played M2–M5 twice end-to-end; six fixes: hotspot scene frame 3:2→5:3 (markers were drifting ~2-3% off-feature), BadgeMedal now resolves real badge art (earned color / unearned grayscale ghost), Toast viewport bottom-right→top-right (5s level-up toast sat on the footer Finish button and ate its clicks), match/sort completion strips made content-neutral (were M2/M3-specific), walkaround review label fix.
2026-08-06 W2 [qa-fix] journey-verify: ride_plan artifact page gained the DESIGN-003 print affordance (shared .ts-print-sheet); M6 module-complete copy branched (was self-referential about the Ride Plan); dashboard Continue after dev-helper jumps points at last-touched lesson per SPEC-006 §Resume — correct as written, noted.
2026-08-06 W2 [settled] Module XP/badge awards when the final checkpoint PUT lands (before "Finish lesson") — server-authoritative by design; the module moment still shows in-session. /verify page says "genuine" not "valid" (VerifyResponse.valid stays the wire field).
2026-08-06 W2 [orchestrator] Wave 2 exit verified: full course playable (M1 W1; M2–M5 twice; M6 + capstone with 5 real prefills), assessment fail→pass, certificate + logged-out verify, graduate dashboard, all 9 badges, instructor page + CSV reflecting live journey data, SSE streaming frame-verified. Gates: tsc/eslint/pytest 59/ruff all clean.
2026-08-06 W2 [qa] Crawl pass 3: 77 shots, 1 skip (first /learn dev cold-compile outran harness patience; wait_for_selector mount guard added). Review in artifacts/crawl/pass-3/REVIEW.md — no P1s; deep-state harness extensions + zero-P1/P2 iteration are Wave 3.
2026-08-06 W3 [journeys] J1–J3 green in 15.8s zero retries (web/e2e/journeys.spec.ts, chromium-only, answer keys parsed read-only from the authored bank); API smoke = 31 table rows in ONE file incl. SSE frames + 429 on a dedicated X-Forwarded-For IP; pytest now 90. Zero data-testids added — roles/labels + data-step-id sufficed. QA-002's "J1 incl. the sort" note: M1 authors no sort; the real tap-sort + wrong-drop assert lives in J2's M6 leg (documented in the spec header).
2026-08-06 W3 [design-qa] Harness now reaches the FULL matrix (92 states, 0 skips): scripted assessment attempts from the authored key, rate-limit drive on a spoofed X-Forwarded-For IP, print-media emulation, skeleton/toast/offline/focus-visible captures, StepRail revisit for behind-frontier steps, mobile-first ordering for fixture discipline. Manifest deep states added mirroring SPEC-010's language; module-overview state/fixture remap (not-started→fresh/m1, partial→mid/m3, complete-badge→mid/m2).
2026-08-06 W3 [design-qa] Pass 4 (run 073124): product zero P1/P2; six P3s swept component-first (ModuleCard nowrap duration, journal-peek date wrap, content revisit chip copy, structured_response post-submit neutral criteria, summit text-balance). Four harness capture-integrity defects fixed (tutor unroute-before-reload persisted a stray turn; Escape ate the toast; print state never emulated; scroll crops). Pass 5 recapture ran clean (run 075623, 92/92) — review pending.
2026-08-06 W3 [design-qa] PASS 5 IS THE EXIT PASS (artifacts/crawl/pass-5/REVIEW.md): all 92 PNGs reviewed — zero P1, zero P2, zero new P3; second consecutive product-clean full pass per QA-001. Traversal audit: 90 page visits as public/mid/grad, every nav/card/chip/source-chip/footer link followed — 0 dead links, 0 wrong destinations, 0 console errors (auth/me 401 + certificate 404 rows are designed status probes, recorded).
2026-08-06 W3 [gates] R9.2 PASS: first-paint gzip 126KB (/login) / 144KB (/dashboard) vs 350KB budget; marked already lazy. Lighthouse desktop 100/100/99 (landing/dashboard/lesson) — dashboard CLS 0.246→0.001: SkeletonGroup's 150ms anti-flash delay collapsed to 0 height, hero popped layout; skeletons now reserve space (visibility:hidden) and track real heights.
2026-08-06 W3 [gates] Keyboard-only pass green (57 focus stops, all with visible ring); fix: closing the Ranger slide-over dropped focus to body — SlideOver forwards onCloseAutoFocus, AppShell returns focus to the trigger. Reduced-motion pass green (all animations ≤0.01ms under the global rule, incl. a genuine level-up toast). Semantics pass green (landmarks, 26 real-slot alts, labeled fields). vite preview.proxy added for prod-build measurement.
2026-08-06 W3 [ops] torch declared directly with [tool.uv.sources] CPU-wheel pin for the linux Docker image (SPEC-012 "torch-cpu wheels" — PyPI default would drag full CUDA into the api image).
2026-08-06 W3 [ops][fix] ops/nginx.conf: added `listen [::]:80;` — the web healthcheck's busybox wget resolves localhost to ::1, and the IPv4-only listener left the container permanently "unhealthy" while serving fine. Both containers healthy after rebuild.
2026-08-06 W3 [launch] SPEC-012 cold-boot acceptance passed live keyless on :8080 (register → M1 lesson → extractive tutor answer with sources; build+up+healthy ≈5 min with cached base images; ≤10 min bar met). SECURE_COOKIES=1 verified on a separate boot (Secure; HttpOnly; SameSite=lax); security headers via curl -I; secrets grep clean. SPEC-008's six questions re-verified live on extractive; anthropic path code-verified — first keyed deploy should re-run the six manually.
2026-08-06 W3 [launch] QA-004 walked box-by-box (artifacts/launch/QA-004-checklist.md) and SPEC-001 R1–R9 all P0+P1 ACs verified (artifacts/launch/SPEC-001-acceptance.md). README gained a Verification-artifacts section.
2026-08-06 W3 [orchestrator][fix] .gitignore negation for artifacts/crawl/*/REVIEW.md was ineffective (git cannot re-include under an ignored dir) — reviews were never actually committed; rule simplified, all five reviews + launch artifacts now tracked.
2026-08-06 W3 [orchestrator] Ran an adversarial verification round (four independent skeptics told to FALSIFY the done-claims, default verdict FAILED). Budget + content fidelity CONFIRMED outright; keyless + learner-journey came back PARTIAL with real defects. Fixing them was worth more than declaring done twice.
2026-08-06 W3 [fix][P2] Dashboard Continue card pointed BACKWARDS once the last-visited lesson was complete: ContinueCard trusted learner_state blindly and its `?? steps.at(-1)` fallback named an answered step "Up next", landing the primary CTA in review mode and contradicting the trail card beside it. Now a useContinueTarget() hook resolves the target from real course data and falls forward (next unfinished lesson → next unlocked module → graduate hero); in-progress resume (R1.2) is byte-for-byte unchanged. Two earlier waves had noticed this and rationalized it as "SPEC-006 §Resume as written" — it wasn't; "Up next" on a completed step is a lie regardless of which spec line you read.
2026-08-06 W3 [fix][P2] Keyless tutor violated ADR-005 §Consequences + SPEC-008 §extractive: the zero-chunk branch returned a refusal-shaped punt with no answer and no nearest-topic pointer, and identical suggestion chips every time. Root cause: shape_retrieval discards sub-floor chunks, so the provider was structurally blind to what the corpus holds. Added nearest_candidate() (pure, floor-ignoring view of the raw top-k) resolved to title + module only when the kept set is empty; the answer now names the closest course topic and the chips derive from it. shape_retrieval/grounding_label semantics and their unit tests untouched. Note: the offending text came verbatim from STARTER/tutor_pipeline.py — a reference implementation loses to an ADR.
2026-08-06 W3 [fix][P2] api container could not boot without network (huggingface_hub revalidated the baked model at startup; with restart: unless-stopped that is a crash loop). ENV HF_HUB_OFFLINE=1 added after the build-time download layer; proven both ways — `--network none` boots and ingests 33 chunks in 10.1s, and the same ENV with an empty cache fails the fetch loudly, so it is load-bearing rather than decorative. README's offline claim rewritten to name both halves (model baked AND runtime revalidation disabled) since the half-reason is what let the bug ship. Image rebuilt so the on-disk one is no longer the pre-fix build.
2026-08-06 W3 [fix][P3] Lesson-not-found state drew the barred-gate state-locked plate under "No lesson lives at this address" — now state-404; the genuinely-locked branch keeps state-locked.
2026-08-06 W3 [fix][P3] Authored final-assessment passPct was inert (hardcoded 80.0); the stored bank value now drives the pass check, falling back to 80.0 only when absent/non-numeric/out of range. Proven by a matrix: stored 95 flips a 90% sheet to failed, stored 60 passes a 70% sheet, junk falls back. ADR-006 content-as-code.
2026-08-06 W3 [fix][P3] Lesson-complete heading said "+N XP this lesson" while counting only the current mount's events, under-reporting a lesson finished across two sittings. Now "+N XP just now" — the number matches the itemized list beside it; the running total lives on /progress. J1's assertion updated with it.
2026-08-06 W3 [fix][P3] Smoke fixture leaked ~1 MB per run: Chroma held sqlite/hnsw handles so rmtree(ignore_errors=True) failed silently. Teardown now stops the Chroma System and clears its registry before removal, and warns loudly if a dir survives; six stale dirs removed, four consecutive runs leave zero.
2026-08-06 W3 [size] ADR-002 §400-line rule, stated reasons for all 12 oversized files (supersedes the two earlier, now-stale counts): api.ts 569 and schemas.py 449 are the single-file SPEC-004 contract on each side of the seam (splitting fragments the contract); progress.py 578 is the mandated single evidence write path covering all 12 renderer contracts plus the completion cascade; seed.py 512 is tokenizer + fail-loud validations + upsert; fixtures.py 662 is hand-written learner prose for three accounts (no placeholder text is allowed in crawl-visible fixtures); pipeline.py 403 carries answer() and its streaming twin sharing helpers so they cannot drift; TutorChat.tsx 439 is one shared chat surface used by both the route and the slide-over; WalkaroundLab.tsx 428 is ~90 lines of authored inline SVG plus a two-stage interaction machine; RenderersSandbox.tsx 1026 is verbatim curriculum payloads by design (DEV-only, verified absent from dist); visual_crawl.py 799, journeys.spec.ts 484 and test_smoke.py 534 are the QA-001/QA-002/QA-003 artifacts themselves, which the budget sizes by coverage, not by lines.
2026-08-06 W3 [settled] Not fixed, with reasons: a stale second tab can overwrite a locked-in prediction (last-write-wins on an idempotent upsert; no XP or completion impact); module-page lessons labelled "Locked" remain reachable by direct URL (SPEC-006 asks for both sequencing and valid deep links — the build honors both, client-side sequencing is presentational); the 80% bar is repeated in four web copy strings that passPct no longer drives (authored value is 80 and SPEC-006 states 80, so the copy is correct today — wiring it would touch five files to change nothing); web/src/lib/modules.ts mirrors module front-matter without a guard test (a guard test is outside the QA-003 ceiling; verified faithful by the content skeptic).
2026-08-06 W3 [qa] Crawl pass 6 after the fixes (artifacts/crawl/pass-6/REVIEW.md): 92 shots, 0 skips, zero P1/P2/P3 — the exit still holds. Re-verified with it: J1–J3 green 15.8s zero retries, pytest 90, ruff/tsc/eslint clean, entry chunk 124 KB gz.
2026-08-06 ASSETS [orchestrator] VISUAL_ASSETS.md added — 267-asset registry + spec-driven generation procedure derived from the local SDXL smoke test. Found SPEC-007's figure block used zero times across 18 content steps, and three Wave-2 plates (scene-helmet-fit, scene-crossing, scene-loading-cargo) referenced by nothing.
2026-08-06 ASSETS [content] Wave V1: the three orphaned plates wired — figure blocks in m3-l1-s1 and m6-l2-s1, shared assetSlot on m6-l2-s2 (ActivityHost renders one for any non-hotspot renderer). Zero new art.
2026-08-06 ASSETS [art] Waves V2–V5: 89 house-style SVG plates generated by four parallel lanes against frozen set specs (12 keylist diagrams, 26 sort icons, 11 match + 6 section marks, 12 activity + 7 topic marks). merge_fragments.py added so lanes write per-batch fragments instead of racing on manifest.json.
2026-08-06 ASSETS [content] Wave V2 wiring: 10 keylist figure blocks added to content, one per step, captions in house voice. Seed parser green (7 passed, still 6/22/59).
2026-08-06 ASSETS [deviate] keylist-helmet-consequences and keylist-good-crossing generated but unwirable — their steps already carry a V1 figure and the one-figure-per-step rule holds. Marked deferred in manifest rather than deleted; cause was a batch brief written from the registry without re-checking the prior wave's result. VISUAL_ASSETS §8.3 amended.
2026-08-06 ASSETS [deviate] C-050..C-055 re-specced diffusion → svg-authored after all 24 SDXL candidates failed: the model renders beautiful generic scenes but will not honour a spatial claim (a junction, a downed machine, riders stopped on one bank). Recorded as VISUAL_ASSETS §6.5 + failure catalogue F13/F14.
2026-08-06 ASSETS [art] Waves V6–V11: 172 slots total (from 27). Lesson cards, hotspot insets, level emblems, artifact covers, XP marks, state art and six hand-authored scenario plates. Every one wired: asset lint reports 169 real / 169 wired / 0 orphaned / 0 problems.
2026-08-06 ASSETS [tools] lint_assets.py (§10.4 asset lint) + merge_fragments.py added. Two bugs in the lint found by lanes, not by me: a hardcoded dynamic-family list let 22 slots pass on a doc-comment mention, and fixing that applied the 8KB glyph budget to 960×540 plates because one list was answering two questions. Documented the limitation that matters — the lint proves a slot is REFERENCED, not that it RENDERS; the crawl is the gate.
2026-08-06 ASSETS [deviate] Scenario plates accepted with residual: seated/standing figures improved, mounted riders are ~30px at plate scale so the redraw barely shows, creek-line still reads strongly blue. Stopped iterating per NON_GOALS §1 rather than spiral; recorded so a later pass starts from here.
2026-08-06 ASSETS [spec] VISUAL_ASSETS §5.4.1 added — the calmer-than-the-prose test: art depicting an incident must be calmer than the text it sits beside, because the learner arrives at the picture through the sentence. Art more vivid than its text is an automatic stop. Also: ask what an asset CLAIMS about the viewer's state, not only what it depicts (hero-graduate was rightly refused on the assessment locked state).
2026-08-06 ASSETS [fix] Crawl pass 7 (P2, in-place review): both hotspot base plates carried a decorative "plate blaze" — a clay rounded diamond at 45°, the exact shape/colour/rotation of the hotspot activity's own INTERACTIVE unvisited-waypoint marker. It read as an extra waypoint that ignores taps, while the counter said "8 of 8 explored". Only 2 of 5 scene plates had one, so it was a leftover, not a convention (clause C4). Removed from both, with the reason left in the SVG so it does not come back. Found only by looking at the render — no lint can see it, because both the plate and the marker are individually correct.
2026-08-06 ASSETS [note] Gallery review and crawl review are different instruments and the crawl wins. The scenario creek I judged too blue at 620px in a review sheet reads restrained at its real ~510px in the field-report card; I had reviewed it at a size it never renders at. Same lesson as §10.4's lint caveat: correct-in-isolation is not correct-in-place.
2026-08-06 ASSETS [art] Identity set B-061..B-068 produced into web/public/ and wired (index.html + site.webmanifest): favicon svg/32/16, apple-touch-icon, maskable 192/512, Safari mask-icon, og-default 1200x630. Replaces an inline data-URI favicon and adds the OG/Twitter tags the app never had — a shared link previewed as bare text. Mark chosen by rendering four candidates at TRUE 16px and comparing: the pine-300 horizon version dies at 16 and only the paper-0 cut survives, because that contrast jump can hold one device pixel. Deliberately NOT in manifest.json — these are static root files, not SlotArt slots, and the lint would call all eight orphaned.
2026-08-06 ASSETS [fix] Two identity-set defects caught by measuring, not looking: Chromium bakes LCD subpixel antialiasing into text-bearing PNGs (912 px of >40 RGB channel spread on a two-colour card; -webkit-font-smoothing does not reach SVG text) — fixed with the Part 1 supersample, x3 + Lanczos, to 0 px; and the favicon PNGs baked white corner pixels that would halo on a dark tab strip, fixed by compositing transparent outside the corner radius while apple-touch-icon stays opaque (iOS fringes on transparency).
2026-08-06 ASSETS [scope] Consumer audit before building the rest of the registry, recorded as VISUAL_ASSETS §8.2.1. The registry specifies 259 rows; the manifest holds 172 slots and EVERY consumer that requests art already gets real art (zero missing slots, zero placeholders). Of 87 outstanding rows only 8 had a free consumer. Skipped with reasons: callout vignettes and tutor grounding art (consumer occupied by DESIGN-002, and displacing spec'd rendering with decoration is a design change I was not asked for), state art (surfaces do not exist — offline is a banner, which is the right smaller design), marketing hero (occupied by hero-landing), social cards + email art (NON_GOALS §2). Refused cert-guilloche and cert-watermark outright: that ornament is anti-counterfeiting language from government credentials and this certificate ships a disclaimer saying it is not a licence — the art would contradict the copy. The registry count is a ceiling, not a target.
2026-08-06 DONE [orchestrator] Definition of done (START_HERE) met: SPEC-001 ACs pass, crawl passes 5 and 6 = zero P1/P2 across the full 92-state matrix, J1–J3 + smoke green (pytest 90), docker compose cold boot keyless — now genuinely network-free — in ≤10 min, QA-004 fully checked. Final gates: tsc 0 / eslint 0 / ruff clean / pytest 90.
2026-08-06 QA [crawl] Pass 8 (post-identity-set): 91 shots, 1 skipped, zero P1/P2 — best run of the build. The single failure (/assessment locked, login click timeout) re-ran in isolation in 0.4s, so it was contention from the crawl's many sequential logins against deliberately-slow argon2id, not a product defect. Checked rather than assumed, because a timeout that looks like a failure and one that is a failure read identically in the log. Also confirmed the ADR-006 content-hash gate reseeds an authored-copy edit on API restart with no manual step.
2026-08-06 ASSETS [art] SET-A-HERO regenerated with the SDXL pipeline: all 9 heroes (landing, 6 modules, assessment, graduate) are now renders rather than flat SVG plates, wired as raster slots. Seed 42 chosen as the house illustrator by reading a reference sheet COLUMN-wise (same seed across different subjects) rather than picking the prettiest single image — that is what makes a 9-plate set read as one series (clause C4).
2026-08-06 ASSETS [deviate] hero-m6-roads ships as an empty forest road. The shipped brief — a trail MEETING a paved road with a machine stopped at the boundary — is a relationship between things, and eight seeds proved it unrenderable in both directions: lead with the rider and no road appears; lead with the road and the machine vanishes. Second independent confirmation of VISUAL_ASSETS 6.5. The hero carries the module's subject; the crossing procedure stays in the lesson's vector keylist where structure can actually be drawn.
2026-08-06 ASSETS [fix] hero-m1's rider rendered a malformed hand (a black antler-shaped blob). Photo inpainting (Telea) and a feathered clone patch were both tried and both smeared, because flat vector art is hard edges and flat fills — any blending is visible. Fixed by column-wise flat reconstruction of the background instead: rebuild structure, never blend it. Recorded because it generalises to every future raster retouch.
2026-08-06 ASSETS [tools] gen_batch.py now resolves the cached SDXL snapshot DIRECTORY instead of the repo id. Neither offline lever works alone on this cache: HF_HUB_OFFLINE=1 still lets diffusers call model_info() first and its fp16 fallback raises, while local_files_only=True demands a COMPLETE snapshot when the fp16 download deliberately skipped 38 files it never needed. Handing from_pretrained a local dir sidesteps Hub resolution entirely, so sandboxed runs behave like shell runs.
2026-08-06 ASSETS [decide] Lesson cards stay svg-authored. Four SDXL candidates were rendered and compared against the shipped SVGs at 96px, the size LessonRow actually uses: THREE OF FOUR became the same picture (a white quad on a forest trail), because what distinguishes those cards is structural — an inspection loop, a stop bar, a tipped machine beside an intact helmet. The one that worked was the helmet: a single object with no relation to render. Recorded as VISUAL_ASSETS 6.6 — diffusion draws SUBJECTS, vector draws RELATIONS; ask what the asset loses if you could only show one object with no arrangement. Also: diffusion cannot count (3 gear items -> a 30-40 item catalogue on every seed), so treat a meaningful quantity exactly like a spatial claim.
2026-08-06 QA [crawl] Pass 9, 89 shots, zero P1/P2 — and the first crawl run against the PRODUCTION BUILD rather than the dev server. The dev crawl lost four /assessment states to net::ERR_INSUFFICIENT_RESOURCES, reproducibly on clean servers: eager import.meta.glob over the art dirs makes the dev server serve 171 SVG + 81 raster rungs as individual modules per page load, exhausting Chromium's connection budget. The build compiles those globs to a static map, so the same crawl against vite preview was clean. Nearly written off as flake after run one; it reproduced. qa/visual_crawl.py now documents crawling the build — it tests what ships and removes the class.
2026-08-10 DESIGN [decide] Design spec v2 supersedes v1 wholesale by owner directive: DESIGN-001..006 + STARTER/design-tokens.css rewritten as "Living Field Guide" — real elevation (3 tinted shadow tiers + glow), layered gradient color, fluid display type, edge-composed bands (no hollow bands, footers everywhere), and motion as a product-wide layer (entrance/scroll choreography, count-ups, draws, ceremonies; 700ms ceiling, spring in ceremonies only, reduced-motion absolute incl. delays). Deliberately retained so v2 is an evolution not a rebuild: brand hues + the 250-asset art program and its contracts, component names, shipped copy strings, lesson-stage geometry, VISUAL_ASSETS authority incl. SF1-SF6. v1 lives in git history; crawl must emulate reduced motion before any reveal work ships (full-page capture never fires IO).
2026-08-10 WEB [build] DESIGN v2 "Living Field Guide" implemented across web/ by a 10-lane orchestrated build (3 foundation + 7 page lanes, disjoint file surfaces) and driven to ZERO P1/P2 over 9 adversarial fix/verify rounds (findings: 70 -> 22 -> 12 -> 3 -> 6 -> 3 -> 1 -> 2 -> 1 -> 0), certified on crawl 20260810-163455 (93 states incl. the first mobile /instructor capture). J1-J3 green first-try; entry chunk 127.66 KB gz vs 350 budget; Lighthouse-relevant paths unchanged. Harness hardened en route: crawl emulates reduced motion (settled-state shots), --base defaults to the build on :4173, /instructor with-data asserts arrival (a mis-provisioned INSTRUCTOR_EMAILS previously photographed the 403 as ok:true), /instructor added to the mobile set. 25 P3 nits batched in the round-9 workflow output for a future polish pass. Working tree uncommitted pending owner review.
2026-08-12 WEB [build] Lesson play layer (DESIGN-004 §Play): hotspot scenes gain a spot-first hunt phase (unmarked scene, tap-to-find with name-tag settles, miss ripples, always-available reveal, arrow-key crosshair for keyboard; revisit skips it — the A-009 acceptance criterion made playable), a lesson-scoped sharp-streak chain in the footer (first-try successes across checkpoints/sorts/matches/spots, milestones 3/5/8, accuracy only per the never-from-speed law), and a clean-run ceremony (badge-tier shine, zero-miss sessions) on sort/match/full-sweep hunts. All derived from existing curriculum payloads; no content invented, no server/schema changes; completion+evidence contracts untouched. Verified by scripted playthrough (m2 anatomy: 8/8 unaided sweep -> ceremony + x8 milestone on screen) with zero page errors.
2026-08-12 WEB [build] Field Practice range (/games, SPEC-010 + DESIGN-004 §Play item 4): a per-module games hub built entirely from authored curriculum payloads — Sharp rounds (module checkpoint questions re-dealt one-shot, up to 5, authored feedback after every pick, miss = ✕ mark not padlock), Walkaround order (T-CLOC from memory off the lab's own zone data), and pure-play replays (hunt/sort/match/branching through the real renderers, evidence=null in, no-op sink out, labeled "nothing is recorded"). Range rules: no clocks anywhere (never-from-speed), locked modules stay locked, bests in localStorage only (ts-practice:{userId}:{gameId}, higher score then clean tiebreak), zero server traffic or evidence writes. Nav "Practice" desktop-only (tab bar keeps 4 stops); module pages link "Field practice". Routes registered in router + SPEC-010 + route-manifest.json (crawl states incl. locked-direct-visit replay). Verified: tsc/eslint/build clean, J1-J3 green, 11-screenshot playthrough reviewed (hub incl. both locked rows, round wrong-pick truth highlight, order clean-run ceremony, hunt replay in spot mode, tab-bar unchanged at 4).
2026-08-16 ART [review] GPT art pack batches 01-10 reviewed one by one at full size against VISUAL_ASSETS Part 5: 93 of 99 accepted. Six rejected, slots keep their shipped SVGs, re-rolls queued: c033 (brand letterforms on gear, U3), c037 (portrait funnel unreadable at the 400x240 canvas), b005 (two stacked panels = mud at 96px, breaks SET-B-LESSON one-object rule), b006 (walkaround loop drew FOUR T-CLOC stops - the course and g003 both say five; C4 set failure with teaching consequence), c127 (metallic-grey technical cutaway, U2 palette break), c055 (phone screen carries baked 'no service' letterforms, U3 - re-roll wants a wordless one-bar glyph). c054, the course's only shipped incident depiction, passed 5.4.1 deliberately: seated rider, helmet on, gear intact, no injury - calmer than its prose; flagged for owner glance in the report.
2026-08-16 WEB [build] GPT art pack integrated: 83 slots to raster ladders (9 heroes incl. the long-impossible a007 crossing, 20 lesson cards, 13 insets, 5 scenario plates, 11 callout vignettes via new CalloutCard art + step:variant map, moments D-007..011, states D-012..016, Tier G games art + R-series range chips registered as VISUAL_ASSETS 7.7), marketing E-001..009 + G-009 exported to web/public/og (per-route OG wiring still pending prerender). slotmap resolves raster (smallest rung webp) so LessonRow/chips work unchanged. D-016 replaces the greyed hero-assessment on the locked state per 5.4.1. Bundle: vite assetsInlineLimit 0 - at HEAD the SlotArt chunk carried 472KB of base64 plates in violation of 9.4; now 48KB of URLs, total JS -40 percent, first-paint 155.5KB gz (budget 350). tsc/eslint/build/asset-lint all clean (207 slots, 0 orphans).
2026-08-16 CONTENT [edit] m1-l1 checkpoint prompt reworded by owner directive: the rider's rationale changes from minimizing ('a short loop') to image ('convinced that's the cooler way to ride'); option-b feedback's closing line updated in step ('Looking cool changes neither'). Teaching point (risk factors multiply) untouched. Re-seeds on next boot via content hash.
2026-08-16 DESIGN [decide] Checkpoint band accent gold -> fireweed by owner directive: sun-400 on the navy band clashed beside the StepRail's clay checkpoint chip. clay-400 lifted #C9628A -> #CE6B92 so it clears 4.5:1 as TEXT on pine-950 (was focus-ring-only at 4.46:1); mapped into the Tailwind theme; checkpoint flag + eyebrow now clay-400. Badge gold and caution semantics keep sun-400 everywhere else; other dark-band gold eyebrows (lesson/module complete, locked assessment) unchanged pending owner review.
2026-08-16 CONTENT [edit] m1-l2-s1 (the judgment leaks) reworded by owner directive for plain human voice at unchanged length and structure: intro, all three keylist details, figure caption (reservoir -> 'a tank with three slow leaks'), and the out-loud tip. The three term NAMES kept deliberately - group gravity / familiarity discount / sunk momentum thread through m1-l2 reflection chips, M4 deadfall hotspot, M5 match, M6 crossing keylist, and two final-assessment feedbacks; each detail now opens with the words a rider actually thinks so the coined name lands. Teaching content unchanged.
2026-08-16 DESIGN [decide] Decorative gold retired from dark bands by owner directive (checkpoint fireweed approved on sight): lesson/module-complete eyebrows, pass banner eyebrow + score + cert codes, locked/intro assessment eyebrows, dashboard graduate eyebrows, and the sun glows on the complete banner and open course card all move to clay-400/clay. Gold now appears ONLY as semantics: caution (strips, callout bars, wrong-answer marks, offline/triage) and earned-badge gold (medals, clean-run ground, award icons, verify panel) - both backed by artwork that carries gold accents.
2026-08-16 ART [retouch] c051 (scenario-creek-pressure) gamma-lifted from near-black night to legible blue-hour dusk (curve 0.62 + color 1.12): the n2 plate swap read as a rendering glitch rather than 'same place, later'. Source PNG untouched in artgen/incoming; ladder re-exported; manifest note records the retouch.
2026-08-16 CONTENT [edit] m1-l2-s2 creek scenario restructured by owner directive into two explicit parts: n1 mid-morning (assess in good light; best-path feedback now resolves the morning - a walked, visible reach - so the day continues), n2 the night return at the same crossing with LOW VISIBILITY as the focus (headlight flattens the water; morning's read expired; risky choice reframed as crossing on memory - the familiarity discount with the lights off). Debrief now names the two-problem structure. Node ids/qualities/flow unchanged; J1's n2 best-choice regex updated to match the new line ('Not in the dark'). The c051 night render is restored un-retouched - the dark image IS the teaching - reverting today's dusk lift; day/night alt text realigned.
2026-08-16 WEB [build] Creek scenario thought layer (owner directive, recovered prompt): per-node field reports land - BranchNode gains optional 'report' (seed parser is shape-permissive, no server change; SPEC-007 addendum noted in types) and the renderer swaps the field-report text on the same beat clock as the plate, so the night return retells the scene. Four tap-to-reveal rider thoughts per creek plate via renderer-local ThoughtLayer + thoughts.ts (pixel-space anchors keyed by plate SLOT, same contract family as the hotspot coordinate tables). Affordance: breathing fireweed ring (registered ambient - DESIGN-004 deviation by owner directive; tokens.css reduced-motion kill collapses it), hover 1.10 grow, one bubble at a time rising on the base curve, real buttons with rider names in aria labels. Self-reviewed by screenshot in both scenes per the owner's own acceptance bar: far-bank bubbles initially escaped the plate's top edge - re-aimed sideways to stay inside the frame; day 'you' marker nudged off the helmet. tsc/eslint/build clean, J1 green.
2026-08-16 WEB [polish] Thought bubbles reshaped as scalloped clouds by owner directive: border-free union of solid paper circles (body + edge lobes + shrinking tail puffs) with the ink outline and lift drawn on the composite silhouette via chained drop-shadow filters (.ts-thought-cloud) - per-element borders would seam the overlaps. Screenshot review caught the right-opening far-bank cloud escaping the plate edge and crowding the crossing marker; both far clouds now open left into the sky pocket (one-at-a-time reveal makes the shared space safe).
2026-08-16 WEB [fix] Thought clouds rebuilt after owner rejected the DOM-circle version on sight (lobes read as warts at full size; tail union crashed into the marker chip) - a lesson in reviewing at 2x crops, not full-page thumbnails. Now one generated SVG path: even scallop arcs traced around the measured text box (useLayoutEffect, so real font metrics), stroked 1.5px pine-950 to match the art program's line weight, stroked tail circles, soft silhouette drop-shadow. Composition pass at 2x: 'you' clouds flipped top-left over open water (top-right crossed the far rider's chip). All four thoughts inspected in both scenes.
2026-08-16 WEB [fix] Thought clouds rebuilt to the traditional convention per owner-supplied analysis (insights adopted, sample code not - style mismatch): the hull is now an ELLIPSE fit to the measured text box, ringed by seed-jittered uneven lobes (stable per text) - no rectangular silhouette, no corners, no straight sides; text width-tuned per thought to wrap 2-3 centred lines so the mass stays oval; trail is detached shrinking circles walking into the thinker's marker chip. Side clouds float above their marker line via per-thought px nudges (percent translate references the text box, not the svg - learned the hard way). All six bubbles re-reviewed at 2x in both scenes against the analysis's acceptance bar, incl. the collisions the tuning pass caught (big day cloud vs the you-chip). Suggested screenshot-regression test declined: QA-003 caps verification at the crawl + J1-J3; the crawl photographs this step.
2026-08-16 WEB [fix] Thought-bubble placement pass against the owner's four annotated screenshots (qa/Screenshot 2026-08-16 *.png): binding rule adopted - a cloud must NEVER cover another rider's marker chip (the X'd case buried far-A's chip unclickably). Clouds slimmed (ellipse 1.24/1.32 + tighter margins - the too-tall complaint), per-thought dx/dy/gap nudges, new bottom-right variant for night far-A (only chip-free window is the dark water below), day far-B relocated up-right per the arrow with a slight comic panel-break over the plate edge. 7 of 8 bubbles verified chip-clear by 2x screenshot (night friend/you geometry mirrors verified day equivalents).
2026-08-16 WEB [fix] Thought-bubble TRAIL made geometric, per the owner's annotated screenshots (dots swallowed by the cloud or stranded in a gap beside the chip). The layer now measures the marker-to-cloud vector, solves where that ray exits the cloud's ellipse, and steps 2-3 shrinking circles along the clear runway - so any cloud placement trails correctly and there are no per-bubble puff offsets left to drift out of sync. Fixed offsets were the root cause: they were measured from the TEXT BOX while the cloud silhouette overhangs it. Night far-A moved (bottom-right -> left, over the mountains) so its trail leads to its own rider, not the neighbour's; day/night far-B gaps widened to 56 for runway. All eight bubbles re-verified at 2x in both scenes: every trail connects chip-to-cloud, no cloud covers a chip.
2026-08-16 CONTENT [edit] m1-l2-s2 field report: 'Your friend behind you shouts' -> 'Your friend to your left shouts', matching where that rider actually sits in the c050/c051 plates (owner). Thought-bubble aria labels updated to match.
2026-08-16 ART [reroll] c051 (scenario-creek-pressure) replaced with an owner-supplied re-roll in which the machines' HEADLIGHTS ARE LIT — the earlier render had four riders sitting in the dark while the node-2 report said "your headlight flattens the creek into one black sheet". Same night, same composition, same 1448x1086 source aspect, so the crop math and the pixel-space thought anchors carried over unchanged (verified by overlay, then by 3x screenshot of all four bubbles on the live build). Source swapped in artgen/incoming/batch-09 under its existing name and the ladder re-exported through the same pipeline (center-crop 16:9 -> Lanczos -> AVIF/WebP/PNG, 1024/512); manifest note records the re-roll, VISUAL_ASSETS C-051 row rewritten to what now ships. Copy follow-on: night far-A's thought "I can't see the other guys anymore." was falsified by the picture (every rider's lights are plainly visible) and becomes "We've got headlights. Just come." — which also puts the headlight illusion in the pressuring rider's head, the exact belief the risky-choice feedback dismantles. Asset lint clean (207 slots, 0 orphans); tsc/build clean.
2026-08-21 ART [audit] Every SVG in web/src/assets/svg rendered with Playwright at display size and reviewed against VISUAL_ASSETS Part 5 for modules 3-6 (modules 1-2 already fixed by owner): ten large plates below bar (one defect family - toaster machines, pill riders, stick figures, leader lines to empty circles, decorative blazes, caret grass, amber suns), one acceptable-but-odd-one-out (stop-assess-communicate), six small-SVG nits, 54 dead SVG files shadowed by raster slots. Audit + master prompt + batch-13 image prompts + animated-SVG briefs in artgen/VISUAL_AUDIT_M3-M6.md; images-only agent brief in artgen/gpt-pack/batches/batch-13-images-only.md.
2026-08-21 ART [review] Batch 13 (11 GPT images via Codex $imagegen) reviewed one by one at full size plus zoomed crops of every count-bearing detail: all eleven accepted. Residuals recorded in manifest notes, none a rubric failure: helmet finger count reads one-to-two (a011), signal glyph has three bars with one filled (c055), low case reads as part of the body (a013), sight lines leave from the machine corners (a012), four-families roots oversized (c007). No text, four wheels everywhere, full gear, no magenta on hotspot plates.
2026-08-21 CONTENT [edit] Six keylist+figure pairs converted to hotspot_figure blocks on the keylist-tcloc pattern (m4-l2-s1 stability, m5-l1-s1 four families, m5-l2-s1 before-you-go, m6-l1-s2 pavement physics, m6-l2-s1 crossing, m6-l3-s1 three standards): keylist terms and details carried verbatim into the stops, medallion centres measured from the plates (navy-ring bbox), seal sizes per medallion, captions unchanged. Four-families walk reordered water -> light -> cold&heat -> dust so the numbering follows the plate's trail-pair/rider-pair split (the original list order carried no meaning). Seed tests 7/7.
2026-08-21 CONTENT [decide] m4-l1-s2 hotspot coordinates re-measured to the new scene-trail-hazards raster (owner directive to integrate): crest 18/30 -> 16.6/11.4, side_slope 30/48 -> 18.9/46.6, wet_clay 55/70 -> 44.9/84.6, loose_over_hard 68/58 -> 74.2/54.2, deadfall 78/44 -> 79.1/31.5, soft_edge 62/78 -> 67.7/83.5; shadow_rut 40/62 unchanged. VISUAL_ASSETS Appendix D treated those numbers as authored and said 'if a marker misses, the art moves' - with a generated scene the art cannot move feature by feature, and a hunt whose markers miss their cues is the worse outcome. Crop (0,0,1536,922) and the numbers are now one contract (Appendix D note). Verified by marker overlay on the build: all seven land on their cues, desktop and mobile.
2026-08-21 WEB [build] Batch 13 integrated: 11 slots to raster ladders (AVIF q72 / WebP q92 / PNG; 3:2 whole for hotspot figures at 1536/768, 5:3 centre crops for plain figures at 1024/512, 16:9 for the scenario plate, 5:2 for the loading-cargo host slot), manifest notes record provenance, crop, residuals and the retired SVG path. scene-loading-cargo's 5:2 crop is the one to remember: ActivityHost renders host-level art at 5/2 and object-cover would have cut a 3:2 image by a third. Gates: lint_assets 217 slots / 212 real / 0 orphaned / 0 problems; tsc + vite build clean (entry 163 KB gz); API booted FIXTURES=1 and reseeded; 30 screenshots of the 11 affected steps on the production build (desktop + mobile, sealed and opened) reviewed, zero page errors. Image generation for the audit is complete; what remains is SVG work - the stop-assess-communicate animated SVG (VISUAL_AUDIT §5.9; its raster fallback ships meanwhile) and the Tier-3 small-SVG fixes (sort-cond-cold-hands, sort-cond-heat, sort-cond-storm, sort-cargo-towing, level-6-wayfinder, badge-b-terrain).
2026-08-22 ART [svg] Remaining SVG work from VISUAL_AUDIT_M3-M6 produced as CANDIDATES in qa/svg (not wired, owner inspects first): seven animated plates (stop-assess-communicate, helmet cutaway, stability plumb-line hub, four families, pavement physics, crossing procedure, loading-cargo tilt) and six icon fixes (sort-cond-cold-hands/heat/storm, sort-cargo-towing, level-6-wayfinder, badge-b-terrain). Built by a two-round orchestrated workflow (13 Opus-max builders + adversarial reviewers, then dossier-driven fix + verify): round 1 left 12/13 on 'fix' because open-ended reviewers never pass; round 2 graded each dossier item before adding findings and all 13 now pass (no P1, <=2 P2). Tools: qa/svg/_check.py (budget, text ban, palette, reduced-motion, ids), _render.py (as-<img> render, reduced-motion render, 12-frame seeked sheet, true-size/zoom), _gallery.py -> index.html, _audit_md.py -> AUDIT.md. Hotspot-figure plates (C/D/E) pin their medallions to the live stop percentages so the curriculum blocks need no edit; the crossing animation is a plain figure (seals would cover the moving quads).
2026-08-22 QA [harness] Two facts learned building qa/svg: Playwright's reduced_motion emulation does NOT reach an SVG loaded through <img> (the media query is silently ignored) - the honest render launches Chromium with --force-prefers-reduced-motion; and seeking CSS animations for frame sheets works by inlining the SVG and setting currentTime on document.getAnimations(). Also: Opus-max builders die on the 64k output ceiling when they echo SVG source into messages - every prompt now carries an output-discipline block.
2026-08-24 WEB [rebuild] Stability lab rebuilt after the owner rejected v1 ("terrible low quality svg", "not realistic physics in ANY way"): outcomes now EMERGE from a matter-js 0.20 rigid-body world (stabilityWorld.ts / stabilityRun.ts — mass, contacts, friction, a breakable seat joint) instead of a quasi-static formula, and the stage draws the course's own painted plates cut to sprites (web/src/assets/stability/atv-side, atv-rear, helmet-side, each with anchor JSON) instead of hand-drawn SVG. 1 world unit = 1 m at 150 px/m; stabilityModel.ts survives only for the free-tilt sandbox and the CoG plumb overlay. The acceptance bar is stabilityRun.check.ts: 415 deterministic in-process runs (~11 ms each) asserting monotonic lean sweeps, minMargin >= 0.08 on clean runs, descent <= 35 deg, standing never worse than seated, and +-0.03 lean robustness, with matter-js global state reset per run because browser and node outcomes diverged without it. Four deviations remain, each printed with its physical reason and accepted (the two that matter are the loaded haul crossing a 0.20 m step on a 26 deg climb, and standing off a 0.50 m descent ledge, where the landing bounce is genuinely not a smooth function of the slider). Contract in qa/stability-rebuild/CONTRACT.md; what actually landed in BUILD-NOTES.md.
2026-08-24 OPS [handover] Handover pass so the project survives its authors. One-click launchers at the repo root (START_SIGHTLINE / STOP_SIGHTLINE / ADD_RANGER_KEY as .sh + .command + .bat + .ps1): the start script installs uv and Python 3.12 if missing, syncs the server, npm ci + builds the web, writes a .env with a freshly generated session secret and REQUIRE_SUBSCRIPTION=0, asks once (hidden input) for the Anthropic key, starts uvicorn on 8000 and vite preview on 8080, then runs ops/bootstrap_accounts.py so a first run ends with a printed sign-in rather than a puzzle. The developer's live-tunnel scripts moved to tools/live/ so nothing at the root is ambiguous about which copy it starts. tools/check-site.sh added: read-only, both ends, exit 0/1, safe in cron. Two constraints the fresh-clone test pinned down and the docs now state: the Node floor is 20, not 18 (on 18 `npm ci` silently skips @tailwindcss/oxide's native binding and the build dies with "Cannot find native binding", a message that never mentions Node), and uv.lock's torch wheels exist only for win_amd64, macosx_14_0_arm64 and manylinux x86_64 — Intel Macs, Windows-on-ARM and macOS below 14 cannot run the server locally at all and are pointed at the live site or DEPLOY.md. Ranger's extractive mode now shows an offline badge in the chat header instead of degrading silently. .gitignore widened over secrets, db snapshots, backups/ and the two untracked handover notes.
2026-08-24 DOCS [rewrite] README rewritten from the Docker-centric original to one screen to first success: the double-click launchers by OS, what first run does and how long, where data lives, the Ranger key, a where-things-are table, the content loop and its corpus caveat, the six checks, and how the live site is ACTUALLY served (laptop + systemd --user + ngrok, not compose — the old production section described a deployment that has never run). New docs/ carries STUDENT-ONBOARDING.md (first day, spec reading order, ten things that bite), HOW-THE-COURSE-IS-BUILT.md (curriculum format, the twelve renderers and the evidence kind each is validated against, assetSlot -> manifest.json -> SlotArt, Ranger end to end) and OPERATIONS.md (units, live scripts, health, journalctl, backups, account resets, a what-to-do-if table). HANDOFF_OSAMA.md cut from 73 lines to 115 words, OSAMA-EMAIL.txt added, both untracked and gitignored. Every command in the new docs was run on this machine first, which turned up three facts worth keeping: ops/bootstrap_accounts.py hardcoded the container's /app/server on sys.path and could not run outside Docker at all (found here, fixed in the handover lane the same day — it now resolves server/ relative to itself); the physics check needs Node 22+ and Playwright needs 20+ while the system node is 18; and there is no sqlite3 CLI on the live machine, so the documented backup uses python's sqlite3 .backup() rather than cp (the db runs in WAL mode, where cp can capture a torn state).
