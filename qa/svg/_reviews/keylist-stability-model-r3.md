# Round-3 review dossier — keylist-stability-model.svg

Final verdict after 3 rounds: **fix**

## Outstanding defects (fix every P1 and P2; P3 where cheap)
### 1. [P1] centre `<use href="#stb-rider" class="stb-rd">` + the whole `#stb-rider` symbol (the handlebar rect x76 y142 w248, both #46555A grips, both navy gloves live INSIDE the rider symbol)
- Where: renders/keylist-stability-model.frames.png t=6s cell; my seek render at t=6.6s (52-58.5% = 6.24-7.02s); AND statically in .img.png / .rm.png in the bottom-left 'position' medallion at (232,585), which applies transform="translate(52 -12)" to the same symbol
- What: `.stb-rd` translates the entire rider group by (-40,-5) symbol units, and that group owns the handlebar, both grips and both gloves. So the bar leaves the steering stem (#stb-quad rect x192 y130), the left glove ends up hanging in open air past the machine's left fender attached to nothing, and a paper-coloured gap opens under the jersey: the torso's closing edge is at symbol y158 (stroke to 159.8) while the body hump top is at y166 (stroke to 164.1) — only ~3.2 world units of clearance at rest, and the -5 y-shift more than doubles it while the -40 x-shift slides 29 units of the torso past the hump's left edge onto nothing. At the 700 px display size this reads as the rider coming off the machine, not leaning uphill. Because the same offset is baked into the position medallion, a detached rider is present in the t=0 frame and in .rm.png — the two frames the reviewer judges as standalone plates. Violates S3/S5/S6 and inverts the safety message of the one beat the rider actually owns.
- Fix: Move the handlebar rect, both grips and both gloves out of `#stb-rider` into `#stb-quad` (or a third `#stb-bars` symbol that never animates), so only helmet/torso/arms move. Then replace the translate with a small rotation of the torso about the seat (transform-box: fill-box; transform-origin at the torso's bottom-centre, symbol 200,158) of about -12deg, and cap any y-shift at 0 so the jersey's bottom edge never leaves the body hump. Apply the identical construction to the position medallion instead of translate(52 -12).

### 2. [P1] `#stb-side` wheels: the 16-segment zig-zag tread path (`M138.6 342.8 142.5 359.6 ...`) plus the `<g fill="#46555A"><circle cx=100 cy=342 r=12/><circle cx=312 cy=346 r=11/></g>` hubs
- Where: top-left 'slopes' medallion at (233,178) in .img.png, .rm.png and every cell of .frames.png; 6x zoom of world x160-320 / y190-300
- What: Each wheel renders as a navy disc carrying a grey OCTAGONAL ring with a grey hub dot at its centre — it reads as a cog/sprocket, not a knobby tire. Section 0.2 bans this outright ('no gear-tooth tires'). The shipped plate web/src/assets/raster/keylist-stability-model-768w.png draws the same side-view wheel as a round black tire with a concentric grey rim and no teeth. It is also a C4 failure inside a single file: the rear-view tires (`#stb-quad`) use chevron notches drawn INSIDE a rounded-rect silhouette, while the side-view tires use an angular ring drawn AROUND a circle — the same object drawn two ways in one plate.
- Fix: Redraw both `#stb-side` wheels as the shipped raster does: navy disc, one concentric `#46555A` rim circle at r ~0.62 of the tire radius (stroke, not a polygon), and 5-6 short chevron notches inside the silhouette using the same `M137 340l17 6 17-6` language as `#stb-quad`. Delete the filled hub dots or reduce them to a plain small navy circle.

### 3. [P1] the four dashed connectors in `<g stroke="#2F6B52" stroke-width="4" stroke-dasharray="10 8">` — paths M356.7 262.9 431.8 314.5 / M843.3 262.9 768.2 314.5 / M370.2 526.8 412 509.2 / M827.8 526.5 787.9 509.6
- Where: .img.png and .rm.png (t=0), and every frame of .frames.png; clearly visible as isolated green dashes in the corners of my t=5.2s / t=6.6s / t=7.9s crops
- What: All four stop in blank paper. The top pair end at (431.8,314.5) and (768.2,314.5) — the nearest ink is the helmet, 138 world units away laterally, roughly 80 px of empty paper at the 700 px display. The bottom pair are only 45 units long (2-3 dashes) and end at (412,509) and (788,510), about 27 px short of the tire flanks, so they read as two stray green ticks rather than connectors. Section 0.2 forbids 'leader lines or arrows that point at nothing', and section C specifies 'dashed spruce connectors TO THE CENTRE GROUP'. The shipped raster runs each connector all the way in to the machine.
- Fix: Extend each connector so it terminates on the centre machine's silhouette: the top pair onto the grips at roughly (523,447) and (677,447), the bottom pair onto the outer tire flanks at roughly (459,560) and (741,560). Keep the 10/8 dash and no arrowheads. If any connector must stay short, all four must stop at the same radius from their medallion so they read as a deliberate device rather than as truncation.

### 4. [P2] `.stb-mgd` — @keyframes stb-m (stroke-dashoffset 74.4 at 62-70%) and @keyframes stb-k (stroke #A93226 from 59%)
- Where: t = 7.44-8.4 s (the load beat) compared against t = 4.86-5.64 s (the amber beat); measured by colour-sampling my 1400 px seek renders
- What: Measured on the render: the amber segment spans world x 655.7 to 737.1 = 81.4 units; the brick segment spans 666.9 to 737.1 = 70.3 units. The 'collapse' is 11.1 world units — 6.5 px at the 700 px display — so the escalation from caution to danger is carried almost entirely by hue. Worse, section C's beat requires the plumb to land PAST the tire edge; here the plumb foot is at x 658.7 while the downhill tire edge is the pivot at 741.1, so the plumb stays 70 units (half of the 141-unit half-track) inside the support base. `#A93226` is reserved for risk semantics, and it is being asserted over a picture that still shows a comfortable margin — the colour and the geometry contradict each other.
- Fix: Raise the load's CoG shift so the plumb actually reaches the edge: change stb-c's 62%,70% keyframe from translate(58.7px,-74px) to roughly translate(135px,-92px) (a taller load, CoG about 100 symbol units up), and set stb-m's 62%,70% stroke-dashoffset to about 97 so the downhill margin collapses to under 10 units. Let the red read as a stub or a pip at the tire edge, not as a quarter-track bar.

### 5. [P2] `#stb-box` — rect x136 y126 w128 h70 fill #ABCDB8, plus its two navy strap rects x158/x240 y122 h78
- Where: centre machine at t = 7.44-8.4 s (my z_load crop at t=7.9s); and statically in the bottom-right 'loads' medallion at (966,585) in .img.png, .rm.png and every frame
- What: The box's top edge (symbol y126) sits at the rider's sternum, 16 units above the handlebar (y142-156). It hides the middle of the bar and both of the jersey's paper panels, and the arms emerge from behind it — at display size it reads as a parcel held against the rider's chest or a chest carrier, not as a load on the rear rack. It is also only 128 of the machine's 332 symbol units wide (39%), and pale `#ABCDB8` against the spruce jersey makes it pop as a rider-side object. The shipped raster's loads medallion draws the load as a wide spruce box across the whole rear deck with the helmet clearly above it. Since the same frame turns the margin brick red, a learner can conclude the hazard is 'carrying a box in your lap' rather than 'a load raises the centre of gravity'.
- Fix: Widen the box to roughly 230 symbol units so it spans the rack-tube footprint (x ~52 to ~282), drop its top edge to about y150 so the jersey and both paper panels stay visible above it, and fill it `#2F6B52` with a navy outline so it reads as cargo on the machine rather than on the rider. Keep the two navy straps but land them on the rack tubes at y182-192.

### 6. [P2] the two navy tick pairs `M-63.8 45.4-69.3 60.4` / `M67.2 93.2 61.7 108.2` (sidehill medallion) and `M-67.2 93.2-61.7 108.2` / `M63.8 45.4 69.3 60.4` (position medallion)
- Where: top-right medallion (967,178) and bottom-left medallion (232,585) in .img.png, .rm.png and every frame
- What: These are the end ticks of a ground bracket whose connecting bar was never drawn. With nothing joining them and no relation to the plumb line, they read as two short strokes stabbed into the hillside outboard of the tires — section 0.2 bans 'unexplained ticks'. They are also inconsistent within the plate: the slopes and loads medallions carry no ticks at all, and the centre machine's support base is drawn as a full bracket with a bar, so the same idea appears in three different notations.
- Fix: Either join each pair with a 3.6-unit navy bar offset perpendicular to the slope (matching the centre machine's tick-bar-tick bracket) so they read as the support base, or delete all four ticks and let the tire contacts carry it, as the slopes and loads medallions already do.

### 7. [P2] bottom-left 'position' medallion at (232,585) as a whole — its ground path `M-160 132 160 16`, its rotate(-20) wrapper, and the rider's translate(52 -12)
- Where: .img.png / .rm.png at the true 700 px display width, compared side by side with the top-right 'sidehill' medallion at (967,178)
- What: Section C's acceptance line requires 'the four medallion cases are recognisable at 0.42 scale'. This one is not. The rider offset is 52 symbol units x 0.42 = 22 medallion units on a machine only 139 units wide — about 13 px at the 700 px display — and the medallion's slope runs the opposite way to the sidehill medallion, so the pair reads as one picture and its mirror. Nothing in the frame shows the CONSEQUENCE of moving uphill (no bracket, no margin, and the plumb foot sits mid-track in both), so the only signal is the small offset, which currently reads as the rider being knocked loose (see the P1 above) rather than deliberately shifting weight.
- Fix: Give the position medallion the SAME slope handedness as the sidehill medallion so the two read as before/after, exaggerate the weight shift to about 90 symbol units of torso lean (with the bars left in place per the P1 fix), and add the payoff: draw the plumb foot near the downhill tire in the sidehill medallion and back at mid-track in the position medallion.

### 8. [P2] `#stb-quad` first `<g fill="#0D1E2E">` — rect x132 y292 w40 h88 rx17 and rect x228 y292 w40 h88 rx17, with their `M137 340l17 6 17-6 ...` notches
- Where: centre machine in .img.png / .rm.png (world x 542-576 and 624-658, y 568-643) and in all three rear-view medallions; most legible in my tilted crops at t=5.2s and t=7.9s
- What: Two short dark rounded stubs hang under the engine, 17 world units clear of the ground line, symmetric about the centreline — exactly where a seated rider's boots would fall. The rider is drawn with no legs and no boots anywhere in the plate, so a learner reading for gear (S2 requires over-ankle boots) will read these as feet; a learner counting wheels (S1) gets an ambiguous answer. The shipped raster leaves that space open, showing A-arms and a skid plate against white, so this is also a divergence from the plate this file must match.
- Fix: Either delete both rects and expose the A-arms and the `#46555A` skid plate as the shipped plate does, or commit to them as front wheels: widen each to at least 56 symbol units, extend them down to the ground line at y400, and give them the full chevron tread and the 4.24 navy outline the rear tires carry so they cannot be mistaken for boots.

### 9. [P3] `.stb-bx` / @keyframes stb-b — opacity 0 to 1 across 60.4-62% and 1 to 0 across 70-71%
- Where: my seek renders at t = 7.05 s, 8.45 s and 8.55 s
- What: The box fades in over 192 ms and out over 120 ms. At t=8.45 s it is a half-transparent ghost with the jersey's paper panels and the handlebar showing straight through it, which breaks the flat-fill language of section 0.2 ('Fills flat'), and 120 ms is a pop rather than a departure under the section 0.3 motion law.
- Fix: Give each transition at least 300 ms (for example opacity 0 at 58%, 1 at 61%, 1 at 70%, 0 at 73%), or keep the box fully opaque throughout and slide it in and out from behind the rack so nothing is ever translucent.

### 10. [P3] `#stb-mark` — `<circle r="16" fill="#F9FCFA" stroke="#0D1E2E"/>`
- Where: centre CoG mark at (600,463) with stroke-width 3; the four medallion CoG marks at scale(0.62) with stroke-width 4.8
- What: The mark's outline renders at 3.0 world units at the centre and 2.98 in the medallions, against the file's otherwise scrupulously maintained 3.6 outline weight — a visible thinning on the one element the eye is meant to land on (U6, section 0.2 'ONE outline weight per file'). Section C also specifies r = 14; the file uses r = 16.
- Fix: Set stroke-width="3.6" on the centre `<use>` and stroke-width="5.8" on the 0.62 medallion instances, and reduce the circle to r=14 to match the brief.

## Reviewer notes
Round 3. I ran _check.py (OK) and _render.py myself and viewed all three outputs, then rendered seven extra paused frames (t = 4.2, 5.2, 6.6, 7.05, 7.9, 8.45, 8.55 s) at 1400 px and at the true 700 px display width, and colour-sampled the margin bar to measure the amber and brick segments in world units rather than by eye. I also viewed keylist-stability-model-768w.png and keylist-tcloc-768w.png and cropped the shipped plate's centre machine, slopes medallion and loads medallion for direct comparison.

Two things I checked and cleared, so they are not in the defect list. First, the machine is genuinely on the ground when tilted: column-sampling t=5.2s puts the lowest tire ink at world y 599.1 with the ground line at 600.0 (left tire) and 649.7 vs 651.4 (right tire) — the apparent float is just the rx=22 rounded tire corners plus the bracket sitting 16 units below the ground line, and the same wedges exist in the level frame. Second, the side-view quad's wheels really are tangent to the medallion slope (computed distance 24.35 vs r 24.36 rear, 22.67 vs 22.68 front) — the wheels' problem is that they are drawn as cogs, not that they are misplaced.

I did NOT flag the 0.42 medallion scale even though the medallion machines fill only 46% of their disc against roughly 80% in the shipped raster, because section C pins 0.42 explicitly. The consequence of that scale is flagged instead, as the P2 on the position medallion. If the owner wants the medallions to match the shipped plate's weight, raising the scale to about 0.60 (wrapper stroke-width 6.0 to keep the 3.6 outline) is the change, and it would also make the position medallion's weight shift legible.

Two smaller things not worth a defect row: `#stb-box` sets stroke-linejoin but no stroke-linecap, so its `M136 146h128` lid line has butt caps against the file's otherwise round caps; and the tread notches inherit the wrapper's 3.6 stroke, so they are the same weight as the outlines, where the shipped raster draws tread lighter than outline.

The t=0 frame does teach on its own — level machine, plumb centred, equal margins, four applications around it — with one exception that matters: the bottom-left position medallion carries the detached rider statically, so the P1 is present in the resting frame and in .rm.png, not only mid-animation. That is why the verdict is fix rather than a motion-only note. Every beat is meaningful and none could be cut; the problem is execution, not structure, and the three P1s are all local repairs that do not disturb the geometry, the timing or the binding medallion coordinates.

## Strengths (keep these intact)
- `_check.py` returns OK: 16,408 bytes against a 25 KB budget, viewBox 1200x800, animated=True. Palette is exactly eight legal tokens (#0D1E2E, #2F6B52, #46555A, #ABCDB8, #ECF3EF, #F9FCFA, #DBA12E, #A93226) — zero magenta, which is the hard requirement for a hotspot plate; no 3-digit hex, no named colours, no currentColor, no <text>, no <foreignObject>, no external references. Title is present as the first child and describes the teaching content, not the decoration.
- Medallion geometry is exact, not approximate: centres at (233,178), (967,178), (232,585), (966,585) with r=150 — 0 units of error against the ±8 tolerance, so the block's four seals will land dead on.
- Reduced motion is correct and provable: renders/keylist-stability-model.rm.png is byte-identical to .img.png (both SHA-256 DEC4057118C2FC59...). Every animated class is switched off in the @media block, and the base presentation attributes (stroke-dashoffset="53", opacity="0" on the box, stroke="#2F6B52" on the margin) carry the rest state without any delayed keyframe doing the work.
- The central claim of the plate is executed correctly: the plumb line genuinely lives outside `.stb-tilt` and stays vertical at every beat (verified in my seek renders at t=5.2, 6.6 and 7.9 s), while `.stb-cog`'s translate keyframes are the exact rotation of (600,463) about the downhill contact (741.1,660) by 13 degrees — (+47.9, -26.7) to the decimal. The mark tracks the machine without ever being inside the rotating group.
- The one-outline-weight rule survives two different scales: 4.24 x 0.85 = 3.604 on the centre machine and 8.6 x 0.42 = 3.612 on every medallion machine, so no symbol hard-codes its own outline and the medallion machines are drawn at the centre machine's weight.
- Motion law is respected: the longest beat is 660 ms (35-40.5% and 70-75.5% of a 12 s loop), the loop period is 12 s with roughly 7.1 s of contiguous rest across the 75.5-100% hold and the 0-35% hold, and the tilt is capped at 13 degrees so the machine never reads as falling — a deliberate and correct restraint the author documented in the source comment.
- The margin bar is internally honest at every beat: the 17-unit gap between the uphill and downhill segments tracks the plumb foot to within a unit (gap midpoint 647.2 vs plumb x 647.9 at the tilt; 658.4 vs 658.7 under load), so the two dashed segments really are 'the bracket either side of the plumb', not a decorative split.

## Constraints and residuals the previous builders reported (do not re-learn these the hard way)
- (builder) prefers-reduced-motion does NOT reach an <img>-embedded SVG in this Chromium — I proved it with a two-colour probe SVG (img and rm pixels identical), so .rm.png is not a genuine reduced-motion render for ANY animated file this harness produces. Worked around by timing the loop so the harness capture window (~3.1s after load) falls inside the rest hold; .img.png and .rm.png now both show the complete state. The @media block itself is correct and fires wherever the query does reach the document. Worth fixing in _render.py (inline the SVG for the rm shot) before other §A–§G files are judged on their .rm.png.
- (builder) §C's beat list says the loaded case happens 'at the same 12°'. With honest geometry (half-track 141 plate units, CoG 228 above ground, +40 for the load) the plumb line cannot reach the tire edge below ~28° — at 12° it only spends a third of the margin. Rather than draw a physical falsehood I split the beat: the box rises at 13° (margin shrinks, amber), then the ground steepens to 30° and the plumb crosses the downhill tire edge (margin brick red). Tilt angles used are 13° and 30°, not 12°.
- (builder) The rear elevation shows the front pair of tires through the gap under the chassis, so they are necessarily narrower and shorter than the rear pair; a strict reading of 'four wheels' is satisfied but the front wheels are foreshortened rather than full-size.
- (builder) In the 'rider position' medallion the rider rotates as a rigid body (-16°, translate -20), so the gloves sit slightly high on one grip and slightly low on the other. At 0.42 scale it reads as leaning, but a reviewer at 4x zoom will see the glove and grip as two adjacent lumps on the uphill side.
- (builder) Seven of the twelve tiles in the contact sheet show the rest state. That is a direct cost of the rest hold needed by both the motion law (≥2s rest) and the capture-window workaround above; the five remaining tiles do cover tilt+amber, lean+spruce, and load+steep+red.
- (fix round 2) [9, deliberate partial deviation] The reviewer asked for the load beat's margin offset to be driven to 100 with the plumb landing outboard of the downhill contact. I could not do that honestly at 13 deg: with a 282-unit track and the CoG 213 units above the bracket, walking the plumb the remaining 141 units needs the CoG about 611 units above the ground - off the top of the machine. So the beat now shows the margin collapsing to 80.8 units (29% of the bracket) in brick red, straight after the corrected state at 131.9 units - a 39% drop plus the colour change - with the plumb still inside. No red tick outboard of the contact point is drawn, because the plumb never lands there. If the owner wants 'the plumb exits' literally, the only honest route is a second, steeper medallion or a tipping-threshold graphic, not a steeper centre machine.
- (fix round 2) [4] Connector lengths are unequal by construction: 91 units for the two upper ones, 44-45 for the two lower ones, because the lower medallions sit ~46 units closer to the composition centre. The standoff circle is common (r=204 about (600,430)) so the gap rule holds at every tilt, but the two lower connectors read as short dashed stubs rather than runs.
- (fix round 2) [4b] For the same reason the gap between endpoint and machine ink is not uniform: ~138 units above (the machine is wide and short, so the circle stands well clear of the helmet) versus 11-47 units at the tire line. A circular standoff around a non-circular subject cannot give both; an ellipse trades this for even shorter lower connectors, which read worse.
- (fix round 2) [3] The side-view quad in medallion 1 now shows only its two near wheels. I removed the 35%-opacity far wheels after the render showed them producing exactly the grey-crescent smudge BRIEFS section L was written to kill. This matches the house side-view convention named in BRIEFS section K ('two visible wheels'), but it means the four-wheel rule is carried by the rear-view symbols, not by that medallion.
- (fix round 2) [1] The rest CoG sits at world (600,463) - the rider's lower torso where it meets the seat - rather than at chest height as in the shipped raster. That placement is what makes the load's rise honest and readable; it is a small, deliberate departure from the raster's mark position.